# new_ccf_function
This piece of R code creates a function that computes cross-correlation values for each lag from -2 to 2, according to the number of valid (not NA) observations available at each lag. The function also computes approximate significance intervals for each lag.
